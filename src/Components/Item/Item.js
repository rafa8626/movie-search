/** @jsx jsx */
// @flow
import { jsx } from '@emotion/core';

/**
 * MovieItem.
 *
 * @returns {React.ReactNode}
 */
const Item = () => (
    <div className="item">
    </div>
);

export default Item;
